{% extends 'base.html.twig' %}

{% block body %}

    <h1>Create new task</h1>

    {{ form_start(form) }}
        {{ form_row(form.title, {'label': 'name of task'}) }}

        <button type="submit" class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}

    <a href="{{ path('task_list') }}">back to list</a>
    
{% endblock %}